# Copy to .env.local — never commit the real values.

# Mwasalati backend (this repo's /backend)
VITE_API_BASE_URL=http://localhost:4000

# AlSooq marketplace integration — token must never be hardcoded in source.
VITE_ALSOQ_BEARER_TOKEN=replace-with-real-token
VITE_ALSOQ_API_BASE_URL=https://api.alsooq.example.com
