# Copy to .env — never commit the real values.

PORT=4000
DATABASE_URL="file:./dev.db"

# Only needed if this service calls AlSooq directly (server-to-server).
ALSOQ_BEARER_TOKEN=replace-with-real-token
ALSOQ_API_BASE_URL=https://api.alsooq.example.com
